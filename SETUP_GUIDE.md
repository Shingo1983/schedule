# JALショッピング比較ツール - 超かんたんセットアップ

パソコン初心者でも大丈夫！画面の写真付きで説明します。

---

## やること一覧（全部で15分くらい）

```
① Pythonをインストールする  ← 5分
② このツールをダウンロードする ← 2分
③ ツールを起動する          ← 1分
④ 楽天のAPIキーを取得する    ← 5分
⑤ 商品を検索してみる        ← すぐ
```

---

## ① Pythonをインストールする

「Python（パイソン）」というソフトが必要です。無料です。

### 手順

1. パソコンで **Google Chrome** や **Microsoft Edge** などのブラウザを開く

2. 上のアドレスバー（URLが表示されるところ）に以下を入力してEnterキーを押す
   ```
   https://www.python.org/downloads/
   ```

3. ページが開いたら、黄色い大きなボタン **「Download Python 3.○○.○」** をクリック
   （○の数字は気にしなくてOK）

4. 画面の下（またはダウンロードフォルダ）にファイルが出るので、それをクリックして開く

5. インストール画面が出る。ここが **一番大事！**
   ```
   ┌─────────────────────────────────────┐
   │                                     │
   │  Install Python 3.x.x               │
   │                                     │
   │  □ Use admin privileges ...          │
   │  ☑ Add python.exe to PATH  ← ★これ！│
   │                                     │
   │  ┌──────────────┐                   │
   │  │ Install Now  │ ← これをクリック    │
   │  └──────────────┘                   │
   └─────────────────────────────────────┘
   ```
   **★「Add python.exe to PATH」の左の四角をクリックしてチェックを入れる！★**
   （これを忘れると動きません）

   チェックを入れたら **「Install Now」** をクリック

6. 「Setup was successful」と出たら **「Close」** をクリック

これでPythonのインストールは完了です！

---

## ② このツールをダウンロードする

1. ブラウザのアドレスバー（上のURLが表示されるところ）に以下をコピペしてEnterキーを押す
   ```
   https://github.com/Shingo1983/schedule/archive/refs/heads/claude/jal-points-shopping-tool-iTAdq.zip
   ```
   （長いですが、全部コピーしてください）
   → 自動でダウンロードが始まります（ボタンを探す必要はありません！）

2. ダウンロードされたファイル（schedule-claude-jal-points-shopping-tool-iTAdq.zip）を探す
   - **Chrome の場合**: 画面の右上にダウンロード表示が出る
   - **Edge の場合**: 画面の右上にダウンロード表示が出る
   - 見つからない場合: キーボードの **Ctrl+J** を押すとダウンロード一覧が開く

3. ZIPファイルを **右クリック** → **「すべて展開」** をクリック
   ```
   ┌──────────────────┐
   │ 開く             │
   │ すべて展開...     │ ← これをクリック
   │ ...              │
   └──────────────────┘
   ```

4. 「展開先の選択」が出たら、そのまま **「展開」** をクリック

5. フォルダが開く。中に **「schedule-main」** というフォルダがある
   → これがツール本体です

---

## ③ ツールを起動する

1. 上で展開した **「schedule-main」フォルダ** を開く

2. 中にある **「start.bat」** というファイルを探す
   （「start」とだけ表示されている場合もあります。歯車のアイコンです）

3. **「start.bat」をダブルクリック**

4. **黒い画面（コマンドプロンプト）** が開く
   ```
   ┌──────────────────────────────────────┐
   │                                      │
   │   JAL LSP ショッピング比較ツール       │
   │                                      │
   │   初回セットアップ中です...            │
   │   起動中... ブラウザが自動で開きます。  │
   │                                      │
   └──────────────────────────────────────┘
   ```
   この黒い画面は **閉じないでください！**（閉じるとツールが止まります）

5. 数秒後、ブラウザが自動で開いてツールの画面が表示される

### もしこんな画面が出たら

**「WindowsによってPCが保護されました」** と出た場合：
```
1. 「詳細情報」という青い文字をクリック
2. 「実行」ボタンをクリック
```
これはWindowsの警告で、問題ありません。

**ブラウザが自動で開かない場合：**
```
ブラウザのアドレスバーに以下を入力してEnter：
http://localhost:5000
```

---

## ④ 楽天のAPIキーを取得する（初回のみ）

「APIキー」とは、楽天の商品データを取得するための許可証のようなものです。
無料で取得できます。楽天会員であれば5分で終わります。

### 手順

1. 新しいブラウザのタブで以下を開く
   ```
   https://webservice.rakuten.co.jp/
   ```

2. **楽天IDでログイン** する
   - 楽天IDがない場合は「楽天会員に新規登録」から作成（無料）

3. ログインしたら、右上のあたりにある **「+アプリID発行」** をクリック

4. 入力欄が出るので、以下のように入力：
   ```
   アプリ名:  価格比較（何でもOK）
   アプリURL: http://localhost（何でもOK）
   ```

5. **「規約に同意して新規アプリを作成」** をクリック

6. **「アプリID/デベロッパーID」** という長い文字列が表示される
   → これをマウスで選択して **コピー**（Ctrl+C）する

7. このツールの画面（ブラウザ）に戻る

8. **「設定」** メニューをクリック

9. 「楽天APIキー」の欄に、コピーしたアプリIDを **貼り付け**（Ctrl+V）

10. **「設定を保存」** ボタンをクリック

これで準備完了です！

---

## ⑤ 商品を検索してみる

1. ツールのトップページに戻る

2. 検索ボックスに欲しい商品の名前を入力する
   ```
   例: AirPods Pro
   例: ルンバ
   例: ダイソン ドライヤー
   ```

3. **「検索」ボタン** をクリック

4. 結果が表示される！
   - **購入推奨**: JALマイレージモール経由がお得かどうか
   - **最安値**: 一番安いショップの情報
   - **比較表**: 各ショップの価格・獲得マイル・実質価格の比較

---

## 2回目以降の使い方

```
「schedule-main」フォルダ内の「start.bat」をダブルクリック
↓
自動でブラウザが開く
↓
商品名を入れて検索
```

それだけです！

---

## 困ったときは

| こうなった | こうする |
|------------|----------|
| 「Pythonが見つかりません」 | ①をやり直す。**「Add python.exe to PATH」のチェック**を忘れずに！ |
| 黒い画面がすぐ閉じる | ①のPythonインストールがうまくいっていない可能性。やり直す |
| ブラウザが開かない | ブラウザのアドレスバーに `http://localhost:5000` と入力してEnter |
| 「APIキーが未設定」 | ④をやる |
| 検索結果が出ない | キーワードを短くする（例:「ダイソン ドライヤー HD08」→「ダイソン ドライヤー」） |
| 「すべて展開」が出ない | 7-Zipなどの解凍ソフトをインストールする |

---

## ツールを終了するには

黒い画面（コマンドプロンプト）の **右上の × ボタン** をクリックして閉じる。
それだけで終了します。
